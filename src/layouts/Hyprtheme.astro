---
import Layout from "./Layout.astro"
import Navbar from "#components/hyprtheme/Navbar.astro"
import Footer from "#components/hyprtheme/Footer.astro"
import Logo from "#assets/hyprtheme-logo.svg"
import type { ComponentProps } from "astro/types"
import type { Except } from "type-fest"

type Props = Except<ComponentProps<typeof Layout>, "icon">

const { title, description = "Easily install themes for Hyprland" } =
  Astro.props
---

<Layout {title} icon={Logo.src} {description}>
  <Navbar />

  <slot />

  <Footer />
</Layout>
